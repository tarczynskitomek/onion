plugins {
    id "io.spring.dependency-management"
    id "java-library"
}

apply plugin: "io.spring.dependency-management"

dependencies {
    implementation(project(":domain"))
    api configurations.springData
}
